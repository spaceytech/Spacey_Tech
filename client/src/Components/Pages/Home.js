import React from "react";

//Components
import Navigation from "../UI/Navigation";

const Home = props => {
  return (
    <div className="container">
      <Navigation />
    </div>
  );
};

export default Home;
